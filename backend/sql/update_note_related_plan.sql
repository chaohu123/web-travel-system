-- ============================================
-- 游记关联路线：为示例游记设置 related_plan_id
-- ============================================
-- 用途：使游记详情页「相关景点推荐」有数据可展示（后端根据关联路线的景点/活动自动生成）
-- 若已执行过 sample_data.sql 且未改过游记 ID，可直接执行本脚本。
-- ============================================

USE `travel_match`;

-- 游记 2（东京 5 日暴走）关联 路线 1（京都奈良文化深度 5 日游）→ 侧栏显示 清水寺、伏见稻荷大社、金阁寺、奈良公园、东大寺 等
UPDATE `t_travel_note` SET `related_plan_id` = 1 WHERE `id` = 2;

-- 游记 3（大理洱海边的慢生活）关联 路线 2（云南大理丽江休闲 6 日）→ 侧栏显示 大理古城、洱海环湖、双廊古镇、苍山索道、喜洲古镇、丽江古城 等
UPDATE `t_travel_note` SET `related_plan_id` = 2 WHERE `id` = 3;

-- 游记 1（冰岛）保持 related_plan_id = NULL，用于展示「暂无相关景点数据」的示例文案
