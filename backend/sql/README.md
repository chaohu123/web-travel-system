# 数据库脚本说明

- **01_schema.sql**：完整建表脚本（创建数据库 `travel_match` 及全部表结构）。先执行此文件。
- **02_data.sql**：完整示例数据插入脚本（用户、路线、结伴、游记、动态、评论及活动经纬度等）。建表后执行此文件。

执行顺序：`01_schema.sql` → `02_data.sql`。

所有示例用户密码均为：`123456`（BCrypt 加密）。
